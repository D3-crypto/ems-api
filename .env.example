# Employee Management System (EMS) Environment Configuration
# Copy this to .env and update the values according to your setup

# Django Settings
SECRET_KEY=django-insecure-50t!=!!5ip-fh013(ym1o(3431@n&u#@wt7wlx$upeq0w*ur@g
DEBUG=True

# Database Configuration
# Set to True to use MongoDB, False to use SQLite
USE_MONGODB=True

# MongoDB Configuration (only needed if USE_MONGODB=True)
MONGODB_CONNECTION_STRING=mongodb://localhost:27017
MONGODB_DATABASE_NAME=EMS

# JWT Token Configuration
JWT_ACCESS_TOKEN_LIFETIME_MINUTES=60
JWT_REFRESH_TOKEN_LIFETIME_DAYS=1

# CORS Configuration
CORS_ALLOW_ALL_ORIGINS=True

# Email Configuration (for OTP sending)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# For production deployment (Render/Railway/etc.)
# ALLOWED_HOSTS=your-domain.com,your-app.onrender.com
# DEBUG=False
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
